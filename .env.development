# === AUTOMATISIERTER DEVELOPMENT-MODUS ===
# Diese Datei wird automatisch verwendet wenn:
# - NODE_ENV=development (Default beim Dev-Server Start)
# - DEMO_MODE=auto (automatische Test-Ausführung)
#
# WICHTIG: Diese Datei IST in .gitignore!
# Sie wird für automatisierte Tests und lokale Entwicklung genutzt.

NODE_ENV=development
PORT=3000
VITE_DEV_PORT=3004

# === AUTOMATISIERTER PROBEBETRIEB ===
# Auto-Start und Datenbank-Sync für E2E-Tests
DEMO_MODE=auto
DB_AUTO_SYNC=true
AUTO_START_SERVER=true

# === LOKALE DATENBANK (MySQL/MariaDB) ===
# WICHTIG: MySQL muss LOKAL läufen oder über Docker verfügbar sein
# Docker Start: docker run -p 3306:3306 -e MYSQL_ROOT_PASSWORD=password -e MYSQL_DATABASE=prostar_db mysql:latest
DATABASE_URL=mysql://root:password@127.0.0.1:3306/prostar_db

# === JWT / SESSION (DEVELOPMENT) ===
JWT_SECRET=dev_secret_key_not_secure_never_use_in_production_12345

# === VITE / CLIENT ===
VITE_APP_ID=prostar_dev_test

# === EMAIL SERVICE (KURS-REGISTRIERUNGEN) ===
# Gmail SMTP für automatisierte Tests
EMAIL_USER=info@prostarmarketing.de
EMAIL_PASSWORD=pefn vhlu yeqm ghll
REPLY_TO_EMAIL=info@prostarmarketing.de
SITE_URL=http://localhost:3000

# === CORS / ERLAUBTE ORIGINS ===
# Im Development: Alles erlaubt
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3004,http://127.0.0.1:3000,http://127.0.0.1:3004

# === DEBUGGING ===
DEBUG=true
LOG_LEVEL=debug

# === STRIPE (Optional, kann auch ohne laufen) ===
STRIPE_SECRET_KEY=sk_test_dev_mode
STRIPE_WEBHOOK_SECRET=whsec_dev_mode

# === TESTING ===
# Automatische Test-Emails
TEST_EMAIL_ENABLED=true
TEST_EMAIL_DOMAIN=talentspring-academy.com
DEMO_MODE_DEFAULT=false

# === OPTIONAL: Azure / Deployment ===
# Falls später zu Azure deployed: Uncomment und ausfüllen
# AZURE_STORAGE_ACCOUNT=
# AZURE_STORAGE_KEY=
# WEBSITE_NODE_DEFAULT_VERSION=20.9.0

# === DB-LOGGING ===
# Details über DB-Operationen anzeigen
DRIZZLE_DEBUG=true
