# === PRODUKTIONS-KONFIGURATION ===
# 🔒 SICHERHEIT: Diese Datei ist in .gitignore — NIEMALS committen!
#
# Verwendung:
#   1. Kopiere diese Datei zu .env.production
#   2. Fülle ALLE Werte ein (siehe Kommentare)
#   3. Setze Permissions: chmod 600 .env.production
#   4. Deployment: NODE_ENV=production npm start
#
# WICHTIG:
#   - Alle Secrets müssen über Umgebungsvariablen gesetzt werden
#   - NIEMALS Secrets in Code oder Git commiten
#   - Verwende Azure Key Vault oder ähnliche Services

# === RUNTIME ===
NODE_ENV=production
PORT=80
VITE_DEV_PORT=

# === DOMAIN & HTTPS ===
# Produktions-Domain (muss mit SSL/TLS laufen)
SITE_URL=https://prostarmarketing.de
ALLOWED_ORIGINS=https://prostarmarketing.de,https://www.prostarmarketing.de,https://app.prostarmarketing.de

# === DATENBANK (Produktions-MySQL/MariaDB) ===
# WICHTIG: Verwende SSH-Tunnel oder privates Netzwerk
# Format: mysql://[user]:[password]@[host]:[port]/[database]
# Beispiele:
#   - Azure MySQL: mysql://admin@servername:Pass123@servername.mysql.database.azure.com:3306/prostar_prod
#   - SSH-Tunnel: mysql://user:pass@localhost:3306/prostar_prod (über SSH-Tunnel auf Port 3306)
DATABASE_URL=

# === JWT / SESSION (Prod Secret) ===
# Generiere einen sicheren Token:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# === VITE / CLIENT ===
VITE_APP_ID=prostar_prod_app

# === GOOGLE ANALYTICS (Optional) ===
VITE_ANALYTICS_ID=

# === EMAIL SERVICE (Kurs-Registrierungen) ===
# Produktions-Email-Konto (separates Konto empfohlen!)
EMAIL_USER=
EMAIL_PASSWORD=
REPLY_TO_EMAIL=support@prostarmarketing.de

# === STRIPE (Zahlungen - Production) ===
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
VITE_STRIPE_PUBLISHABLE_KEY=

# === OAUTH / AUTH ===
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
VITE_OAUTH_PORTAL_URL=

# === OPTIONAL: SendGrid als Email-Backup ===
# Falls Gmail SMTP nicht zuverlässig ist
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@prostarmarketing.de

# === SENTRY (Fehler-Tracking) ===
SENTRY_DSN=

# === ADMIN ===
# Sicherer Admin-Key für geschützte Routen
ADMIN_KEY=

# === LOGGING ===
DEBUG=false
LOG_LEVEL=info

# === SECURITY ===
# Rate-Limiting (pro IP)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# === OPTIONAL: Azure App Service ===
# Falls zu Azure deployed
WEBSITE_NODE_DEFAULT_VERSION=20.9.0
WEBSITE_RUN_FROM_PACKAGE=1
WEBSITE_ENABLE_SYNC_UPDATE_SITE=true

# === OPTIONAL: CDN & Caching ===
# Falls CloudFlare oder ähnliches genutzt wird
CDN_ENABLED=true
CACHE_CONTROL_MAX_AGE=3600

# === BACKUP & DISASTER RECOVERY ===
# Optional: Automatische DB-Backups
BACKUP_ENABLED=false
BACKUP_SCHEDULE=0 2 * * * # 2 AM täglich
